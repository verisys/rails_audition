<p id="notice"><%= notice %></p>

<h1>Car Details</h1>

<table>
  <thead>
    <tr>
      <th>Year</th>
      <th>Make</th>
      <th>Model</th>
      <th>Engine</th>
      <th>Color</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @car_details.each do |car_detail| %>
      <tr>
        <td><%= car_detail.year %></td>
        <td><%= car_detail.make %></td>
        <td><%= car_detail.model %></td>
        <td><%= car_detail.engine %></td>
        <td><%= car_detail.color %></td>
        <td><%= link_to 'Show', car_detail %></td>
        <td><%= link_to 'Edit', edit_car_detail_path(car_detail) %></td>
        <td><%= link_to 'Destroy', car_detail, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Car Detail', new_car_detail_path %>
