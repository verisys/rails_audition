<%= form_for(@department) do |f| %>
  <% if @department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@department.errors.count, "error") %> prohibited this department from being saved:</h2>

      <ul>
      <% @department.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
  <%=f.label :supervisor %><br>
  <%= f.collection_select(:supervisor_id, @supervisor.order('name'), :id, :name, :include_blank => "Please select") %> | <%= link_to 'New Supervisor', new_supervisor_path %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
 